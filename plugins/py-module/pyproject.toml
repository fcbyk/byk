[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "byk-pym"
version = "2026.6.21"
description = "byk example plugin"
requires-python = ">=3.10"
dependencies = [
    "click>=8.0.0",
]