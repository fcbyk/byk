[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "byk-pyb"
version = "2026.6.22"
description = "byk example plugin"
requires-python = ">=3.10"
dependencies = [
    "click>=8.0.0",
]

[project.scripts]
hello4 = "bykpyb.main:hello4"